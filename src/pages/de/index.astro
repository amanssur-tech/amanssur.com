---
export const prerender = false;
import BaseLayout from '../../layouts/BaseLayout.astro';

const { lang = 'de' } = Astro.props;
const page = 'home'; // serializable key, not a component
---
<BaseLayout lang={lang} page={page} />